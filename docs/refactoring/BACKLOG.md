# 리팩토링 백로그

> 최종 업데이트: 2026-01-20  
> 상태: Phase 4 검증 완료 후 정리

---

## 📋 백로그 항목

### 🔴 CRITICAL (즉시 수정 권장)

#### WEB-05: DataTable 컴포넌트 분리 (436줄 → 5파일)

**우선순위**: P1 (단기)  
**현재 상태**: 대기

**문제**:
- 복합 컴포넌트 기준 150줄을 **3배 초과** (436줄)
- 7가지 책임 혼재 (테이블, 검색, 컬럼 가시성, 페이지네이션, 행 선택, 로딩/에러, 정렬)

**권장 해결 방안**:
```
DataTable/
├── DataTable.tsx (~100줄) - 메인 컴포넌트
├── DataTableToolbar.tsx (~50줄) - 검색 + 컬럼 가시성
├── DataTableBody.tsx (~80줄) - 테이블 바디 + 스켈레톤
├── DataTableFooter.tsx (~40줄) - 선택 정보 + 페이지네이션
└── data-table-utils.ts (~80줄) - 유틸리티 함수들
```

**완료 조건**:
- [ ] 5개 파일로 분리
- [ ] 기존 사용처에서 동일하게 동작
- [ ] 빌드 통과

**예상 소요**: 2시간

---

#### SRV-05: jwt-auth.guard.ts any 타입 제거

**우선순위**: P1 (단기)  
**현재 상태**: 대기

**문제**:
```typescript
handleRequest(err: any, user: any, info: any): any  // 3개의 any
```

**권장 해결 방안**:
```typescript
override handleRequest<TUser = TokenPayload>(
  err: Error | null, 
  user: TUser | false, 
  info: { message?: string } | undefined
): TUser
```

**완료 조건**:
- [ ] any 타입 모두 제거
- [ ] 타입 체크 통과
- [ ] 인증 동작 정상

**예상 소요**: 30분

---

#### WEB-07: menu.store.ts 하드코딩 URL 수정

**우선순위**: P1 (단기)  
**현재 상태**: 대기

**문제**:
```typescript
// 현재 - 하드코딩
const response = await fetch('http://localhost:4000/api/menus/my', {...});
```

**권장 해결 방안**:
```typescript
// 수정 후 - apiClient 사용
import { apiClient } from '@/lib/api/client';
const response = await menusApi.getMyMenus();
```

**완료 조건**:
- [ ] apiClient 또는 menusApi 사용
- [ ] 환경별 URL 자동 적용
- [ ] 메뉴 로딩 정상 동작

**예상 소요**: 15분

---

### 🟠 HIGH (우선 수정 권장)

#### WEB-06: MainSidebar 컴포넌트 분리 (275줄 → 4파일)

**우선순위**: P2 (중기)  
**현재 상태**: 대기

**문제**:
- 레이아웃 기준 200줄 초과 (275줄)
- 4개 컴포넌트가 한 파일에 존재

**권장 해결 방안**:
```
layout/
├── MainSidebar.tsx (~100줄) - 메인 컨테이너
└── sidebar/
    ├── CollapsedSidebar.tsx (~40줄)
    ├── ExpandedSidebar.tsx (~60줄)
    └── SidebarSection.tsx (~40줄)
```

**완료 조건**:
- [ ] 4개 파일로 분리
- [ ] 사이드바 동작 정상
- [ ] 빌드 통과

**예상 소요**: 1시간

---

#### SRV-06: response.interceptor.ts any 타입 제거

**우선순위**: P2 (중기)  
**현재 상태**: 대기

**문제**:
```typescript
intercept(...): Observable<any>
```

**권장 해결 방안**:
```typescript
intercept<T>(...): Observable<T>
```

**완료 조건**:
- [ ] 제네릭 타입 적용
- [ ] 타입 체크 통과

**예상 소요**: 20분

---

#### TYPE-05: 메뉴 타입 통합 (@ssoo/types)

**우선순위**: P2 (중기)  
**현재 상태**: 대기

**문제**:
- `packages/types/src/menu.ts`와 `apps/web/src/types/menu.ts`에 동일한 구조 중복 정의

**권장 해결 방안**:
- `@ssoo/types`에 통합 정의
- `apps/web`에서 import 경로 변경

**완료 조건**:
- [ ] @ssoo/types에 메뉴 타입 통합
- [ ] apps/web에서 import 변경
- [ ] 빌드 통과

**예상 소요**: 30분

---

### 🔲 기존 백로그 (P2-P3)

#### WEB-01: 레거시 PageHeader 완전 제거

**우선순위**: P2 (중기)  
**현재 상태**: @deprecated 추가 완료, 내부 사용 중

**설명**:
- `components/common/PageHeader.tsx` (레거시)
- 현재 `FormPageTemplate`, `DetailPageTemplate` 내부에서 사용 중
- 새 표준 `components/common/page/PageHeader.tsx`로 마이그레이션 필요

**완료 조건**:
- [ ] FormPageTemplate에서 새 PageHeader 사용
- [ ] DetailPageTemplate에서 새 PageHeader 사용
- [ ] 레거시 PageHeader 파일 제거
- [ ] 빌드 통과

**예상 소요**: 30분

---

#### WEB-02: 레거시 ListPageTemplate 이름 정리

**우선순위**: P2 (중기)  
**현재 상태**: @deprecated 추가 완료, 참조용 유지

**설명**:
- `ListPageTemplateV2` → `ListPageTemplate`으로 이름 변경
- 레거시 `ListPageTemplate` 파일 제거
- 모든 import 경로 업데이트

**완료 조건**:
- [ ] ListPageTemplateV2를 ListPageTemplate으로 rename
- [ ] 레거시 ListPageTemplate.tsx 제거
- [ ] templates/index.ts export 정리
- [ ] 모든 사용처 import 경로 업데이트
- [ ] 빌드 통과

**예상 소요**: 20분

---

#### TYPE-04: packages/types Index Export 정리

**우선순위**: P3 (장기)  
**현재 상태**: 대기

**설명**:
- `packages/types/src/index.ts`의 export 구조 개선
- 카테고리별 re-export 정리

**완료 조건**:
- [ ] 카테고리별 export 그룹화
- [ ] 타입 체크 통과
- [ ] 빌드 통과

**예상 소요**: 10분

---

## 📌 백로그 관리 규칙

1. **추가 시점**: 리팩토링 중 발견된 개선 사항
2. **처리 시점**: 관련 기능 개발 시 함께 처리
3. **우선순위 기준**:
   - P1: 기능/보안 영향 → 다음 스프린트 필수
   - P2: 코드 품질 → 관련 개발 시 처리
   - P3: 개선 사항 → 여유 시 처리

---

## 📊 백로그 요약

| 우선순위 | 개수 | 예상 소요 |
|:--------:|:----:|:---------:|
| P1 (단기) | 3개 | 2시간 45분 |
| P2 (중기) | 5개 | 3시간 |
| P3 (장기) | 1개 | 10분 |
| **총계** | **9개** | **약 6시간** |

---

## ✅ 완료된 항목

| 항목 | 완료일 | 비고 |
|------|--------|------|
| (아직 없음) | | |

