# SSOO 모노레포 에이전트 가이드 (AGENTS)

> 최종 업데이트: 2026-02-03  
> 범위: SSOO 모노레포 전체 (`hwista-ssoo/`)

---

## 이 문서의 목적

새로운 에이전트(AI 또는 개발자)가 SSOO 모노레포 작업을 시작할 때 **반드시 먼저 읽어야 하는 가이드**입니다.

이 문서는:
- 모노레포 **구조와 각 앱/패키지의 역할**을 설명합니다
- **핵심 원칙과 그 이유**를 설명합니다
- **코드 패턴 표준**과 왜 그 패턴을 따라야 하는지 설명합니다
- 작업 전 **확인해야 할 체크리스트**를 제공합니다

---

## ⚠️ AI 에이전트 작업 원칙

### 역할 분담
| 단계 | AI | 사람 |
|------|-----|------|
| **점검/분석** | ✅ 수행 | 결과 확인 |
| **브리핑/제안** | ✅ 수행 | 검토 |
| **삭제/변경 결정** | ✅ 판단 제시 | 🔒 **최종 승인** |
| **실행** | ⏸️ 승인 대기 | 🔒 **컨펌 후 지시** |

### 점검 기준 (러프하게 넘어가지 말 것)
1. **"수정 필요없음"으로 넘어가지 말 것** - 변경 소지가 있으면 반드시 명시
2. **모든 발견 사항에 증거 포함** - 파일 경로, 라인 번호, grep 결과
3. **영향 범위 명시** - 수정 시 영향받는 다른 파일/패키지 목록
4. **바이브 코딩 산물 의심** - 통제 없이 생성된 코드일 가능성 항상 고려

---

## 🏗️ 모노레포 구조

### 프로젝트 개요

| 항목 | 값 |
|------|-----|
| **프로젝트명** | SSOO (삼삼오오) |
| **목적** | SI/SM 조직의 Opportunity-Project-System 통합 업무 허브 |
| **구조** | 모노레포 (pnpm workspace + Turborepo) |
| **아키텍처** | 모듈러 모놀리스 (도메인별 모듈 분리) |

### 앱/패키지 구성

```
hwista-ssoo/
├── apps/
│   ├── server/              # NestJS 백엔드 API 서버
│   └── web/
│       ├── pms/             # PMS 프론트엔드 (Next.js 15)
│       └── dms/             # DMS 프론트엔드 (독립 프로젝트)
├── packages/
│   ├── database/            # Prisma ORM, DB 스키마
│   └── types/               # 공유 TypeScript 타입
├── docs/                    # 문서
└── [config files]           # 루트 설정 파일들
```

### 패키지별 역할과 제약

| 패키지 | 역할 | 패키지 매니저 | 상태 |
|--------|------|---------------|------|
| `apps/server` | NestJS API 서버 | pnpm | ✅ 활성 |
| `apps/web/pms` | PMS 프론트엔드 | pnpm | ✅ 활성 |
| `apps/web/dms` | DMS 프론트엔드 | **npm (독립)** | 🔄 개발중 |
| `packages/database` | Prisma 스키마, 트리거 | pnpm | ✅ 활성 |
| `packages/types` | 공유 타입 정의 | pnpm | ✅ 활성 |

> ⚠️ **DMS 독립성**: DMS는 npm을 사용하며, `@ssoo/*` 패키지를 참조하지 않습니다.
> DMS 작업 시에는 `apps/web/dms/docs/development/AGENTS.md`를 참조하세요.

---

## 🔴 핵심 원칙

### 1. 코드 클렌징 원칙

> **바이브 코딩으로 생긴 통제 불가 코드를 정리하고, 설계 안에서 컴팩트한 구현에 집중**

| 원칙 | 이유 |
|------|------|
| **사용되는 코드만 유지** | 미사용 코드는 유지보수 부담, 혼란 유발 |
| **불필요한 추상화 제거** | 과도한 레이어는 복잡도만 증가 |
| **미래 기능용 선제작 금지** | 필요할 때 만드는 것이 더 효율적 |
| **일관된 패턴 유지** | 동일한 문제는 동일한 방식으로 해결 |

### 2. 문서-코드 동기화

> **문서에 없으면 코드도 없어야 함, 코드에 없으면 문서도 없어야 함**

| 규칙 | 이유 |
|------|------|
| **코드 변경 → 문서 업데이트 → 커밋** | 문서 부채 방지 |
| **Backlog/Changelog 섹션 유지** | 변경 이력 추적 |

### 3. 패키지 경계 준수

> **각 패키지는 명확한 책임을 가지며, 의존성 방향을 지킨다**

```
apps/server ──→ packages/database
     ↓                 ↓
apps/web/pms ──→ packages/types
```

| 규칙 | 이유 |
|------|------|
| **apps → packages 방향만 허용** | 순환 의존 방지 |
| **packages 간 최소 의존** | 변경 영향 최소화 |
| **DMS는 독립** | 별도 저장소 분리 가능성 |

---

## 📜 작업 표준 프로세스 (필수 준수)

### 1. 변경 작업 흐름

```
코드 변경 → 문서 업데이트 → 빌드 검증 → 커밋
```

| 단계 | 내용 |
|------|------|
| **1. 코드 변경** | 패턴 표준 준수, 영향 범위 파악 |
| **2. 문서 업데이트** | 관련 문서의 Backlog/Changelog 갱신 |
| **3. 빌드 검증** | 영향받는 패키지 빌드 확인 |
| **4. 커밋** | 변경 내용과 문서를 함께 커밋 |

### 2. 삭제/수정 작업 흐름

```
분석 → 브리핑 → 승인 대기 → 실행 → 검증 → 기록
```

| 단계 | 내용 |
|------|------|
| **1. 분석** | grep 검색으로 사용처 확인, 영향 범위 파악 |
| **2. 브리핑** | 대상, 사유, 영향 파일/패키지 목록 보고 |
| **3. 승인 대기** | 🔒 **사용자 컨펌 필수** |
| **4. 실행** | 승인 후 변경 수행 |
| **5. 검증** | 빌드/런타임 테스트 |
| **6. 기록** | Changelog에 변경 내역 기록 |

### 3. 빌드 검증 명령어

```bash
# 개별 패키지 빌드
cd apps/server && pnpm build
cd apps/web/pms && pnpm build
cd apps/web/dms && npm run build  # DMS는 npm

# 전체 빌드 (루트에서)
pnpm build

# 린트
pnpm lint
```

---

## 🏗️ 개발 표준 (필수 준수)

### 1. 프론트엔드 레이어 아키텍처

```
pages → templates → common → ui
  ↓
hooks → lib/api → stores
```

**의존성 규칙**:
- 상위 레이어는 하위 레이어만 참조
- 역방향 참조 금지 (ui → pages ❌)
- 순환 참조 금지

### 2. 백엔드 모듈 구조 (NestJS)

```
modules/
├── {domain}/
│   ├── {domain}.module.ts
│   ├── {domain}.controller.ts
│   ├── {domain}.service.ts
│   └── dto/
│       ├── create-{domain}.dto.ts
│       └── update-{domain}.dto.ts
```

### 3. 컴포넌트 크기 가이드

| 유형 | 권장 라인 | 초과 시 조치 |
|------|----------|-------------|
| UI 컴포넌트 | ~50줄 | 분리 검토 |
| Common 컴포넌트 | ~150줄 | 책임 분리 |
| Template | ~200줄 | 하위 컴포넌트 추출 |
| Page | ~150줄 | 로직을 훅/스토어로 이동 |
| NestJS Service | ~300줄 | 도메인 분리 검토 |

### 4. 네이밍 규칙

| 대상 | 규칙 | 예시 |
|------|------|------|
| **컴포넌트** | PascalCase | `FileTree.tsx`, `ProjectCard.tsx` |
| **훅** | camelCase, use 접두사 | `useAuth.ts`, `useProject.ts` |
| **유틸** | camelCase | `formatDate.ts`, `cn.ts` |
| **타입** | PascalCase | `User`, `Project` |
| **상수** | UPPER_SNAKE_CASE | `DEFAULT_PAGE_SIZE` |
| **NestJS 클래스** | PascalCase + 접미사 | `UserService`, `AuthController` |
| **DTO** | PascalCase + Dto | `CreateUserDto`, `UpdateProjectDto` |

### 5. Export 규칙

```typescript
// ✅ 명시적 re-export
export { Button } from './Button';
export { Input } from './Input';

// ❌ 와일드카드 export 금지
export * from './components';
```

---

## 📋 코드 패턴 표준

### 프론트엔드 미들웨어 (`middleware.ts`)

```typescript
// ✅ 표준 패턴
export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;
  
  const allowedPaths = ['/'];
  if (allowedPaths.some((path) => pathname === path)) {
    return NextResponse.next();
  }
  
  return NextResponse.rewrite(new URL('/not-found', request.url));
}

export const config = {
  matcher: ['/((?!api|_next|.*\\..*|favicon.ico).*)'],
};
```

**왜 이 패턴인가?**
- `export function` (named export) → 일관성
- `matcher` 설정으로 정적 파일/API 필터링 → 불필요한 호출 방지
- 함수 내부 필터링 최소화 → 코드 단순화

### 프론트엔드 스토어 (Zustand)

```typescript
// ✅ 표준 패턴
interface TabStore extends TabStoreState, TabStoreActions {}

const useTabStore = create<TabStore>()(
  persist(
    (set, get) => ({
      // 상태
      tabs: [],
      activeTabId: null,
      
      // 액션
      openTab: (options) => { ... },
      closeTab: (id) => { ... },
    }),
    { name: 'tab-store', storage: createJSONStorage(() => localStorage) }
  )
);
```

### 프론트엔드 ListPageTemplate

```typescript
// ✅ 표준: pagination은 table 내부에 포함
<ListPageTemplate
  breadcrumb={['메뉴', '목록']}
  header={{ ... }}
  table={{
    columns,
    data,
    pagination: {
      page,
      pageSize,
      total,
      onPageChange: setPage,
      onPageSizeChange: setPageSize,
    },
  }}
/>
```

### 백엔드 NestJS Controller

```typescript
// ✅ 표준 패턴
@Controller('users')
@ApiTags('Users')
export class UserController {
  constructor(private readonly userService: UserService) {}

  @Get()
  @ApiOperation({ summary: '사용자 목록 조회' })
  async findAll(@Query() query: FindUsersDto) {
    return this.userService.findAll(query);
  }

  @Post()
  @ApiOperation({ summary: '사용자 생성' })
  async create(@Body() dto: CreateUserDto) {
    return this.userService.create(dto);
  }
}
```

### 백엔드 NestJS Service

```typescript
// ✅ 표준 패턴
@Injectable()
export class UserService {
  constructor(private readonly prisma: PrismaService) {}

  async findAll(query: FindUsersDto) {
    return this.prisma.user.findMany({
      where: { ... },
      skip: query.skip,
      take: query.take,
    });
  }

  async create(dto: CreateUserDto) {
    return this.prisma.user.create({
      data: dto,
    });
  }
}
```

---

## 🔍 Dead Code 삭제 기준

### 삭제 판단 기준
1. **grep 검색 결과 0건** → 어디서도 참조 안 됨
2. **import는 있으나 실제 호출 없음** → 사용 안 됨
3. **주석 처리된 코드 블록** → 필요하면 git에서 복원
4. **TODO/FIXME만 있고 구현 없는 스텁** → 미래 기능 선제작

### 삭제 전 검증
```bash
# 1. 영향 패키지 빌드
pnpm build --filter=<package-name>

# 2. 전체 타입 체크
pnpm typecheck

# 3. 린트
pnpm lint
```

### 완료된 정리 사례 (참고)

**2026-02-02 DMS Dead Code 정리**:
| 삭제 대상 | 라인 수 | 사유 |
|----------|--------|------|
| 미사용 API Handler 9개 | ~500줄 | users, git, watch 등 사용처 없음 |
| AI 관련 코드 | ~200줄 | embeddings, vectorStore 미사용 |
| 서비스 레이어 인프라 | ~1,500줄 | BaseService, 미래 기능용 선제작 |

---

## ✅ 작업 전 체크리스트

### 필수 확인
- [ ] 이 문서(AGENTS.md)를 읽었는지 확인
- [ ] 작업 대상 패키지 파악 (PMS? Server? Database?)
- [ ] DMS 작업이면 `apps/web/dms/docs/development/AGENTS.md` 참조
- [ ] 관련 표준 문서 확인

### 코드 변경 시
- [ ] 기존 패턴과 일치하는지 확인
- [ ] 타입 정의와 구현 일치하는지 확인
- [ ] 불필요한 코드 추가하지 않았는지 확인
- [ ] 영향받는 패키지 빌드 테스트

### 문서 변경 시
- [ ] Backlog/Changelog 섹션 업데이트
- [ ] 관련 문서 링크 유효성 확인

---

## 📁 문서 구조

### 공통 문서: `docs/common/`

| 문서 | 경로 | 역할 |
|------|------|------|
| **이 문서** | `AGENTS.md` | 모노레포 에이전트 가이드 |
| **리팩토링 표준** | `architecture/refactoring-audit-prompt.md` | 리팩토링 감사 기준 |
| **기술 스택** | `architecture/tech-stack.md` | 기술 선택 기준 |
| **개발 표준** | `architecture/development-standards.md` | 코딩 규칙 |
| **모듈러 모놀리스** | `architecture/modular-monolith.md` | 아키텍처 원칙 |
| **문서 관리** | `architecture/docs-management.md` | 문서화 규칙 |
| **보안 표준** | `architecture/security-standards.md` | 보안 규칙 |
| **인증 시스템** | `architecture/auth-system.md` | 인증/인가 설계 |
| **DB 패키지 스펙** | `architecture/database-package-spec.md` | DB 설계 규칙 |
| **서버 패키지 스펙** | `architecture/server-package-spec.md` | 백엔드 설계 규칙 |
| **워크플로우** | `architecture/workflow-process.md` | 개발 프로세스 |

### 도메인별 문서

| 도메인 | 위치 | 설명 |
|--------|------|------|
| **PMS** | `docs/pms/` | PMS 전용 문서 |
| **DMS** | `apps/web/dms/docs/development/` | DMS 정본 (독립) |
| **DMS 통합** | `docs/dms/` | 모노레포 통합 관련만 |

---

## 📊 현재 상태 (2026-02-03)

### 완료된 작업
| 작업 | 내용 |
|------|------|
| **PMS-DMS 구조 통일** | 폴더 구조, 컴포넌트 패턴 표준화 |
| **Dead Code 정리** | DMS 미사용 코드 ~2,000줄 삭제 |
| **패턴 표준화** | 미들웨어, 스토어, 서비스, 템플릿 패턴 통일 |
| **문서 체계화** | AGENTS, 리팩토링 표준 문서 작성 |

### 진행 중
| 항목 | 상태 |
|------|------|
| 문서별 Backlog/Changelog 적용 | 🔄 진행중 |
| 문서-코드 일치성 검증 | 🔄 진행중 |

---

## Backlog

| ID | 항목 | 우선순위 | 상태 |
|----|------|----------|------|
| MR-01 | 전체 문서 Backlog/Changelog 섹션 적용 | P1 | 🔄 진행중 |
| MR-02 | PMS/Server/Database 코드 패턴 점검 | P1 | ⬜ 대기 |
| MR-03 | 전체 Dead Code 분석 (Server, Database) | P2 | ⬜ 대기 |

---

## Changelog

| 날짜 | 변경 내용 |
|------|----------|
| 2026-02-03 | typecheck 명령어 삭제 (스크립트 미존재), 날짜 현행화 |
| 2026-02-02 | 모노레포 AGENTS 최초 작성 |
