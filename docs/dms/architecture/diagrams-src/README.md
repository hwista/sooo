# Architecture Diagrams (Source)

- Place Mermaid (`.mmd`) and PlantUML (`.puml`, `.plantuml`, `.pu`, `.uml`) sources here.
- Generated SVGs are written to `docs/dms/reference/architecture/diagrams`.
- Render command (root): `pnpm docs:architecture`
- Renderer uses Kroki; set `KROKI_URL` if not running at `http://localhost:8000`.
