# 페이지 레이아웃 설계

> 표준 페이지 템플릿별 레이아웃 상세 설계

**최종 업데이트:** 2026-01-19  
**버전:** 1.1

---

## 📋 목차

0. [메인 레이아웃 구조](#0-메인-레이아웃-구조)
1. [목록 페이지 (ListPageTemplate)](#1-목록-페이지-listpagetemplate)
2. [등록/수정 페이지 (FormPageTemplate)](#2-등록수정-페이지-formpagetemplate)
3. [상세 페이지 (DetailPageTemplate)](#3-상세-페이지-detailpagetemplate)

---

## 0. 메인 레이아웃 구조

### 전체 레이아웃 다이어그램

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                              메인 헤더 (60px, #003876)                            │
│  [🔍 통합 검색... (준비 중)          ]              [+ 새 프로젝트] [🔔] [👤 ▼]  │
├─────────────────┬────────────────────────────────────────────────────────────────┤
│   사이드바 헤더  │                        탭바 (53px, gray-50)                    │
│  (60px, #003876)│    [📄 대시보드 ×] [📄 고객요청 ×] [📄 프로젝트 ×]             │
│  [S] SSOO   [≡] │                                                                │
├─────────────────┼────────────────────────────────────────────────────────────────┤
│                 │                                                                │
│   검색 영역     │                                                                │
│  [🔍 메뉴검색]  │                                                                │
├─────────────────┤                     컨텐츠 영역                                 │
│                 │                                                                │
│  ▼ 즐겨찾기     │                    (페이지 템플릿)                              │
│    - 대시보드   │                                                                │
│    - 고객요청   │                                                                │
├─────────────────┤                                                                │
│                 │                                                                │
│  ▼ 열린 탭     │                                                                │
│    - 프로젝트   │                                                                │
├─────────────────┤                                                                │
│                 │                                                                │
│  ▼ 메뉴        │                                                                │
│    📁 요청      │                                                                │
│      - 고객요청 │                                                                │
│    📁 프로젝트  │                                                                │
│                 │                                                                │
└─────────────────┴────────────────────────────────────────────────────────────────┘
```

### 레이아웃 크기 정의

| 영역 | 크기 | 색상 | 비고 |
|------|------|------|------|
| **메인 헤더** | 60px | `#003876` | 통합 검색 + 액션 버튼 |
| **사이드바 헤더** | 60px | `#003876` | 로고 + 접기 버튼 |
| **사이드바 (펼침)** | 340px | `#DEE7F1` | 검색, 즐겨찾기, 열린탭, 메뉴 |
| **사이드바 (접힘)** | 56px | `#DEE7F1` | 아이콘만 표시 |
| **탭바** | 53px | `gray-50` | MDI 탭 영역, 사이드바 검색과 수평 정렬 |
| **컨트롤 높이** | 36px | - | 버튼, 입력, 탭, 메뉴 항목 |

### 색상 체계

| 요소 | 색상 | HEX |
|------|------|-----|
| 헤더 배경 | Primary | `#003876` |
| 사이드바 배경 | Content Background | `#DEE7F1` |
| 사이드바 우측 보더 | Content Border | `#9FC1E7` |
| 내부 구분선 | Gray | `gray-200` |
| 선택 상태 | Content Border | `#9FC1E7` |
| Hover 배경 | Sitemap Background | `#F6FBFF` |

---

## 1. 목록 페이지 (ListPageTemplate)

### 1.1 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ 🏠 > 요청 > 고객요청 관리                                        │ ← Breadcrumb
│ ─────────────────────────────────────────────────────────────── │
│ 고객요청 목록                                    [+ 등록]       │ ← PageHeader
│ 고객사로부터 접수된 프로젝트 요청을 관리합니다                    │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ [프로젝트명 ▼] [검색어입력...  ] [상태 ▼]  [🔍검색] [↺초기화] │ │ ← 검색필터 Card
│ └─────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ □ │ 프로젝트명 ↕ │ 고객사  │ 단계    │ 등록일  │ ⋮        │ │ ← DataTable
│ ├───┼───────────────┼─────────┼─────────┼─────────┼──────────┤ │
│ │ □ │ A 고객 요청   │ A사     │ 🟡 대기  │ 01-19  │ ⋮        │ │
│ │ □ │ B 시스템 구축 │ B사     │ 🔵 진행  │ 01-18  │ ⋮        │ │
│ │ □ │ C 유지보수    │ C사     │ 🟢 완료  │ 01-17  │ ⋮        │ │
│ └─────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│  페이지당 [10▼]개  1-10 / 총 45개          [<<] [<] 1/5 [>] [>>]│ ← Pagination
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 컴포넌트 구성

| 영역 | 컴포넌트 | Props | 설명 |
|------|----------|-------|------|
| 상단 | PageHeader | title, breadcrumb, actions | 페이지 제목 + 브레드크럼 + 액션 버튼 |
| 검색 | Card + 검색필터 | filterFields, filterValues, onFilterChange | 검색 조건 입력 영역 |
| 테이블 | DataTable | columns, data, loading, pagination | 데이터 그리드 (정렬/선택) |
| 하단 | Pagination | page, pageSize, total, onPageChange | 페이지 네비게이션 |

### 1.3 테이블 컬럼 정의

#### 고객요청 목록 예시

| 컬럼 | accessorKey | 타입 | 너비 | 정렬 | 기능 |
|------|-------------|------|------|------|------|
| 선택 | - | checkbox | 40px | ❌ | 다중 선택 (enableRowSelection) |
| 프로젝트명 | projectName | text | flex | ✅ | 클릭 시 상세 이동, 정렬 가능 |
| 고객사 | customerId | text | 120px | ✅ | 고객사명 표시 |
| 단계 | stageCode | badge | 80px | ✅ | 대기/진행/완료 (색상 구분) |
| 등록일 | createdAt | date | 100px | ✅ | YYYY-MM-DD 형식 |
| 작업 | - | actions | 60px | ❌ | 수정/삭제 드롭다운 메뉴 |

**Badge 색상 규칙:**
- 🟡 대기 (waiting): `bg-yellow-100 text-yellow-800`
- 🔵 진행 (in_progress): `bg-blue-100 text-blue-800`
- 🟢 완료 (done): `bg-green-100 text-green-800`

### 1.4 검색 필터 정의

#### 고객요청 목록 필터

| 필드 | key | 타입 | 너비 | placeholder | options |
|------|-----|------|------|-------------|---------|
| 프로젝트명 | projectName | text | 200px | 프로젝트명 입력 | - |
| 상태 | stageCode | select | 150px | 전체 | 대기/진행/완료 |
| 고객사 | customerId | select | 200px | 전체 | 고객사 목록 (API) |

**Enter 키 동작:** 프로젝트명 입력 필드에서 Enter 시 자동 검색

### 1.5 코드 예시

```tsx
<ListPageTemplate
  header={{
    title: '고객요청 목록',
    description: '고객사로부터 접수된 프로젝트 요청을 관리합니다',
    breadcrumb: ['요청', '고객요청 관리'],
    actions: [
      { 
        label: '등록', 
        icon: <Plus />, 
        onClick: () => router.push('/request/customer/create') 
      }
    ]
  }}
  filterFields={[
    { key: 'projectName', label: '프로젝트명', type: 'text', width: '200px' },
    { 
      key: 'stageCode', 
      label: '상태', 
      type: 'select', 
      width: '150px',
      options: [
        { label: '대기', value: 'waiting' },
        { label: '진행중', value: 'in_progress' },
        { label: '완료', value: 'done' }
      ]
    }
  ]}
  filterValues={filters}
  onFilterChange={handleFilterChange}
  onSearch={handleSearch}
  onFilterReset={handleReset}
  columns={columns}
  data={requests}
  loading={isLoading}
  onRowClick={(row) => router.push(`/request/customer/${row.id}`)}
  pagination={{
    page,
    pageSize,
    total,
    onPageChange: setPage,
    onPageSizeChange: setPageSize
  }}
/>
```

---

## 2. 등록/수정 페이지 (FormPageTemplate)

### 2.1 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ 🏠 > 요청 > 고객요청 관리 > 등록                                 │ ← Breadcrumb
│ ─────────────────────────────────────────────────────────────── │
│ 고객요청 등록                                                   │ ← PageHeader
│ 새로운 고객 요청을 등록합니다                                    │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ ▌기본 정보 ─────────────────────────────────────────────── │ │ ← FormSection
│ │                                                             │ │
│ │  프로젝트명 *                                               │ │
│ │  ┌─────────────────────────────────────────────────────┐   │ │
│ │  │                                                     │   │ │
│ │  └─────────────────────────────────────────────────────┘   │ │
│ │  프로젝트명은 2자 이상이어야 합니다 (힌트/에러)              │ │
│ │                                                             │ │
│ │  고객사                                                     │ │
│ │  ┌──────────────────────────┐                              │ │
│ │  │ 선택                   ▼ │                              │ │
│ │  └──────────────────────────┘                              │ │
│ ├─────────────────────────────────────────────────────────────┤ │
│ │ ▌상세 정보 ─────────────────────────────────────────────── │ │ ← FormSection
│ │                                                             │ │
│ │  요청 내용                                                  │ │
│ │  ┌─────────────────────────────────────────────────────┐   │ │
│ │  │                                                     │   │ │
│ │  │                                                     │   │ │
│ │  │                                                     │   │ │
│ │  └─────────────────────────────────────────────────────┘   │ │
│ │                                                             │ │
│ ├─────────────────────────────────────────────────────────────┤ │
│ │                                  [취소]  [💾 저장]          │ │ ← FormActions
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 컴포넌트 구성

| 영역 | 컴포넌트 | Props | 설명 |
|------|----------|-------|------|
| 상단 | PageHeader | title, breadcrumb | 페이지 제목 + 브레드크럼 |
| 폼 | FormSection[] | title, description, children | 섹션별 필드 그룹화 |
| 필드 | FormField | label, required, error, hint | 라벨 + 입력 + 에러/힌트 |
| 하단 | FormActions | onSubmit, onCancel, loading | 액션 버튼 그룹 |

### 2.3 폼 섹션 정의

#### 고객요청 등록/수정 예시

| 섹션 | 필드명 | 컴포넌트 | 필수 | 유효성 검증 | 힌트 |
|------|--------|----------|------|-------------|------|
| **기본 정보** | 프로젝트명 | Input | ✅ | 2자 이상 | - |
|  | 고객사 | Select | ❌ | - | 향후 필수로 변경 |
| **상세 정보** | 요청 내용 | Textarea | ❌ | - | 고객 요청 상세 내용 입력 |

**유효성 검증 규칙 (Zod):**
```typescript
const createRequestSchema = z.object({
  projectName: z.string()
    .min(2, '프로젝트명은 2자 이상이어야 합니다')
    .max(100, '프로젝트명은 100자 이하여야 합니다'),
  customerId: z.string().optional(),
  description: z.string().optional()
});
```

### 2.4 액션 버튼

| 버튼 | variant | 위치 | 동작 |
|------|---------|------|------|
| 취소 | outline | 우하단 | 목록으로 돌아가기 (변경 사항 경고) |
| 저장 | default | 우하단 | 유효성 검증 → API 호출 → 목록 이동 |
| 삭제 | destructive | 좌하단 | 수정 페이지에만 표시, 확인 다이얼로그 |

**로딩 상태:**
- 저장 버튼 클릭 시 스피너 표시 + 버튼 비활성화
- 폼 전체 비활성화 (중복 제출 방지)

### 2.5 코드 예시

```tsx
<FormPageTemplate
  header={{
    title: '고객요청 등록',
    description: '새로운 고객 요청을 등록합니다',
    breadcrumb: ['요청', '고객요청 관리', '등록']
  }}
  sections={[
    {
      key: 'basic',
      title: '기본 정보',
      children: (
        <>
          <FormField label="프로젝트명" required error={errors.projectName}>
            <Input {...register('projectName')} />
          </FormField>
          <FormField label="고객사">
            <Select {...register('customerId')}>
              <SelectTrigger>
                <SelectValue placeholder="선택" />
              </SelectTrigger>
              <SelectContent>
                {customers.map(c => (
                  <SelectItem key={c.id} value={c.id}>{c.name}</SelectItem>
                ))}
              </SelectContent>
            </Select>
          </FormField>
        </>
      )
    },
    {
      key: 'detail',
      title: '상세 정보',
      children: (
        <FormField label="요청 내용" hint="고객 요청 상세 내용 입력">
          <Textarea {...register('description')} rows={5} />
        </FormField>
      )
    }
  ]}
  onFormSubmit={handleSubmit(onSubmit)}
  onCancel={() => router.back()}
  submitLabel="저장"
  loading={isSubmitting}
/>
```

---

## 3. 상세 페이지 (DetailPageTemplate)

> 향후 구현 예정

### 3.1 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ 🏠 > 요청 > 고객요청 관리 > 상세                                 │ ← Breadcrumb
│ ─────────────────────────────────────────────────────────────── │
│ A 고객 요청 프로젝트                          [수정] [삭제]     │ ← PageHeader
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ ▌기본 정보                                                  │ │ ← DetailSection
│ ├─────────────────────────────────────────────────────────────┤ │
│ │  프로젝트명      │ A 고객 요청 프로젝트                     │ │
│ │  고객사          │ A사                                      │ │
│ │  단계            │ 🟡 대기                                   │ │
│ │  등록일          │ 2026-01-19                               │ │
│ │  등록자          │ 홍길동                                    │ │
│ │  수정일          │ 2026-01-19                               │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ ▌요청 내용                                                  │ │ ← DetailSection
│ ├─────────────────────────────────────────────────────────────┤ │
│ │  기존 시스템 노후화로 인한 신규 시스템 구축 요청...          │ │
│ │                                                             │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 컴포넌트 구성

| 영역 | 컴포넌트 | Props | 설명 |
|------|----------|-------|------|
| 상단 | PageHeader | title, breadcrumb, actions | 제목 + 수정/삭제 버튼 |
| 섹션 | DetailSection[] | title, children | 정보 카드 그룹 |
| 필드 | DetailFields | fields, columns | 라벨-값 그리드 레이아웃 |

### 3.3 상세 섹션 정의

#### 고객요청 상세 예시

| 섹션 | 필드 | 표시 형식 | 변환 로직 |
|------|------|----------|----------|
| **기본 정보** | 프로젝트명 | text | - |
| (2열 그리드) | 고객사 | text | customerId → 고객사명 |
|  | 단계 | badge | stageCode → 라벨+색상 |
|  | 등록일 | date | ISO → YYYY-MM-DD |
|  | 등록자 | text | createdBy → 사용자명 |
|  | 수정일 | date | ISO → YYYY-MM-DD |
| **요청 내용** | 메모 | text block | 전체 너비, 줄바꿈 유지 |

### 3.4 코드 예시

```tsx
<DetailPageTemplate
  header={{
    title: data.projectName,
    breadcrumb: ['요청', '고객요청 관리', '상세'],
    actions: [
      { label: '수정', onClick: () => router.push(`/request/customer/${id}/edit`) },
      { label: '삭제', variant: 'destructive', onClick: handleDelete }
    ]
  }}
  sections={[
    {
      key: 'basic',
      title: '기본 정보',
      children: (
        <DetailFields
          columns={2}
          fields={[
            { label: '프로젝트명', value: data.projectName },
            { label: '고객사', value: data.customer?.name },
            { label: '단계', value: <Badge>{getStageLabelvalue}</Badge> },
            { label: '등록일', value: formatDate(data.createdAt) },
            { label: '등록자', value: data.createdBy?.name },
            { label: '수정일', value: formatDate(data.updatedAt) }
          ]}
        />
      )
    },
    {
      key: 'content',
      title: '요청 내용',
      children: (
        <p className="whitespace-pre-wrap">{data.description || '-'}</p>
      )
    }
  ]}
  loading={isLoading}
  error={error}
  onRetry={refetch}
/>
```

---

## 📝 페이지별 적용 현황

| 페이지 | 경로 | 템플릿 | 상태 | 비고 |
|--------|------|--------|------|------|
| 고객요청 목록 | /request/customer | ListPageTemplate | 🔲 예정 | Task #17 |
| 고객요청 등록 | /request/customer/create | FormPageTemplate | 🔲 예정 | Task #17 |
| 고객요청 상세 | /request/customer/[id] | DetailPageTemplate | 🔲 예정 | 향후 구현 |

---

## 🎨 공통 스타일 가이드

### 간격 시스템
- 페이지 상단: `space-y-6`
- 섹션 내부: `space-y-4`
- 필드 간: `gap-4`
- 카드 패딩: `pt-6`, `p-6`

### 색상 규칙
- Primary: `bg-primary text-primary-foreground`
- Destructive: `bg-destructive text-destructive-foreground`
- Muted: `text-muted-foreground`
- Border: `border-border`

### 타이포그래피
- 페이지 제목: `text-2xl font-bold`
- 섹션 제목: `text-lg font-medium`
- 필드 라벨: `text-sm font-medium`
- 설명: `text-sm text-muted-foreground`

---

## 🔄 업데이트 로그

| 날짜 | 버전 | 변경 내용 |
|------|------|----------|
| 2026-01-19 | 1.0 | 초기 문서 작성 (목록/폼/상세 페이지) |
